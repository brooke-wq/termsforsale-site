# Environment / secrets — never commit
.env
.env.*
!.env.example

# Node / tooling
node_modules/
.DS_Store
*.log

# Build outputs (keep repo clean)
dist/
build/

# Runtime state files written by senders/cron jobs on the droplet
# (jobs/sent-log.json is updated on every blast; tracking it caused
# constant divergence between droplet and main)
jobs/sent-log.json
